# No API keys are required — all data comes from FRED's public CSV endpoint and
# multpl.com. Copy to .env only if you want to tune the defaults below.

# Network resilience (raise on slow links).
# HTTP_TIMEOUT=30
# HTTP_RETRIES=4

# Backend host/port.
# SERVER_HOST=127.0.0.1
# SERVER_PORT=8000
